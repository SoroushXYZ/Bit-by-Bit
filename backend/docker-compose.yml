version: '3.8'

services:
  backend:
    build: .
    container_name: bit-by-bit-backend
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-development}
    restart: unless-stopped

